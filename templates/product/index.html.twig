{% extends 'base.html.twig' %}

{% block title %}Hello ProductController!{% endblock %}

{% block body %}

    <div class="row mt-5">
        <div class="col-3">
            <div class="card">
                <div class="card">
                    {{ form_start(form) }}
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
        <div class="col-9">
            <div class="row gy-4">
                {% for article in articles %}
                <div class="col-4 ">
                <div class="card shadow">
                    <div class="card-body">
                        <h5 class="card-title">{{ article.name }}</h5>
                        <p class="card-text">
                            {{ article.description[:25] }}
                            {% if article.description|length > 25 %}
                                ...
                            {% endif %}</p>
                        <a href="#" class="btn btn-primary"> Détails de l'article </a>
                    </div>
                </div>
            </div>
            {% endfor %}
        </div>
    </div>

{% endblock %}
